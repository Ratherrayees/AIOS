import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Intelligence — Analytics — AIOS",
};

export default function AnalyticsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
