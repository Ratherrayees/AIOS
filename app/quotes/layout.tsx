import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sales — Quotes — AIOS",
};

export default function QuotesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
