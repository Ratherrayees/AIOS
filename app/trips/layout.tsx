import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Travel — Trips — AIOS",
};

export default function TripsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
