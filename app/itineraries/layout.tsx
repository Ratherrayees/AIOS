import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Travel — Itineraries — AIOS",
};

export default function ItinerariesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
