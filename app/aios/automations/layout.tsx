import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "AIOS — Automations",
};

export default function AutomationsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
