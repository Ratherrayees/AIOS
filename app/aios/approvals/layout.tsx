import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "AIOS — Approvals",
};

export default function ApprovalsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
