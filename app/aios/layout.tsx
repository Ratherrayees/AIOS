import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "AIOS — Control & approvals",
};

export default function AiosLayout({ children }: { children: React.ReactNode }) {
  return children;
}
