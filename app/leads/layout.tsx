import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sales — Leads & pipeline — AIOS",
};

export default function LeadsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
