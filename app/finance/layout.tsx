import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Operations — Finance & suppliers — AIOS",
};

export default function FinanceLayout({ children }: { children: React.ReactNode }) {
  return children;
}
