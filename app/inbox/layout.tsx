import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Today — Inbox — AIOS",
};

export default function InboxLayout({ children }: { children: React.ReactNode }) {
  return children;
}
