import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Intelligence — Knowledge — AIOS",
};

export default function KnowledgeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
