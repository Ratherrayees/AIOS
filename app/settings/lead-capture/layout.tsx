import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Administration — Lead capture — AIOS",
};

export default function LeadCaptureLayout({ children }: { children: React.ReactNode }) {
  return children;
}
