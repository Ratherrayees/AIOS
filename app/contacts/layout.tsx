import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sales — Contacts — AIOS",
};

export default function ContactsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
