import type { Metadata } from "next";

export const metadata: Metadata = { title: "Usage & limits — Platform administration — AIOS" };

export default function PlatformUsageLayout({ children }: { children: React.ReactNode }) {
  return children;
}
