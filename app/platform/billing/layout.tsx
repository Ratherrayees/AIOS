import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Plans & billing — Platform administration — AIOS",
};

export default function PlatformBillingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
